<%= form_for(@artist) do |f| %>
  <% if @artist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@artist.errors.count, "error") %> prohibited this artist from being saved:</h2>

      <ul>
      <% @artist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :genre_id %><br>
    <% # I could have replaced Genre.all below with @genres if I added the reference to "new" in artists_controller.erb %>
    <%= f.collection_select :genre_id, Genre.all, :id, :name %>  
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
